def predict_project(project_data):
    """
    Numerical prediction layer.

    Later this will load the trained ML model
    for cost and time estimation.
    """

    return {
        "predicted_cost": None,
        "predicted_time": None,
    }